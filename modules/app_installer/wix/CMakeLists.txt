set(CPACK_GENERATOR WIX)
set(CPACK_PACKAGE_VERSION "${VERSION_MAJOR}.${VERSION_MINOR}.${VERSION_BUILD}.${VERSION_REVISION}")
set(CPACK_WIX_UPGRADE_GUID "3C8E68FA-2A9B-438A-8918-26631DD64C17")
set(CPACK_WIX_TEMPLATE "${CMAKE_CURRENT_SOURCE_DIR}/wix.template.in")
set(CPACK_OUTPUT_FILE_PREFIX "${CMAKE_SOURCE_DIR}/deploy")
set(CPACK_WIX_EXTENSIONS WixUtilExtension)

set(VERSION_PRODUCT_NAME "App")
set(VERSION_PRODUCT_LANGUAGE "1033")
set(APP_EXE_NAME "app.exe")
set(APP_SERVICE_NAME "app")
set(APP_ICON "${CMAKE_SOURCE_DIR}/modules/app/src/res/app.ico")

configure_file(
	${CMAKE_CURRENT_SOURCE_DIR}/config.wix.cmake
	${CMAKE_CURRENT_BINARY_DIR}/config.wix
	)

include(CPack)